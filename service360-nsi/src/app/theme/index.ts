/** Файл: src/app/theme/index.ts
 *  Назначение: централизованное подключение темing-настроек (Naive UI, CSS-переменные).
 *  Использование: вызовите setupAppTheme() при инициализации приложения для применения глобальных тем.
 */
export function setupAppTheme(): void {
  // Заглушка: добавьте подключение тем и глобальных CSS-переменных при необходимости.
}
