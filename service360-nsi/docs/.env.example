# ===== Scenario A: backend под /api =====
# БАЗА (без завершающего /). В проде axios пойдёт сюда напрямую.
VITE_API_BASE=http://localhost:8080/api
# Dev-прокси Vite (обычно оставляем /api). В dev переписывает на VITE_API_BASE.
VITE_API_DEV_PROXY_BASE=/api
# RPC endpoint (с ведущим /). В проде даст http://.../api (или из BASE).
VITE_RPC_PATH=/api
# Путь логина (с ведущим /). В типовом случае /auth/login → http://.../api/auth/login если сервер так ожидает.
VITE_AUTH_LOGIN_PATH=/auth/login

# ===== Scenario B: backend под /dtj/nsi (внутри свой /api) =====
# БАЗА (без завершающего /). Пример: http://host:8080/dtj/nsi
VITE_API_BASE=http://localhost:8080/dtj/nsi
# В dev фронт ходит на /api → прокси → http://host/dtj/nsi/api/...
VITE_API_DEV_PROXY_BASE=/api
# RPC (с ведущим /). В проде получится http://host/dtj/nsi/api
VITE_RPC_PATH=/api
# Логин по умолчанию вне /api → /dtj/nsi/auth/login.
# Если ваш логин ВНУТРИ API, замените на строку ниже.
VITE_AUTH_LOGIN_PATH=/auth/login
# VITE_AUTH_LOGIN_PATH=/dtj/nsi/api/auth/login
